<%= form_for :comment, :url => "comments" do |f| %>
  <h4>Leave a comment...</h4>
  <div>
    <div><%= f.text_area :text, placeholder: "write your comment..."%></div>
  </div>
  <%= f.submit "Comment" %>
<% end %>
