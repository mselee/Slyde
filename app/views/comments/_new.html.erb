<%= form_for :comment, :url => comments_create_path do |f| %>
  <h4>Leave a comment...</h4>
  <div>
    <label>text</label>
    <div><%= f.text_area :text %></div>
  </div>
  <%= f.submit "Send" %>
<% end %>
